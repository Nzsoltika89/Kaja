<div class="col-md-4">
<div class="border" style=" outline: 1px groove #010;">

  <h3><%=link_to post.title, post_path(post.id) %></h3>
  <h3><%= sanitize post.description %>
<small>
      <time><%= post.created_at.to_date.strftime("%d %B, %Y") %></time>
</small>
</div>
</div>